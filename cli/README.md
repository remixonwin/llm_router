Interactive CLI for the Intelligent LLM Router

Quick start:

1. cd cli
2. npm install
3. npm start

The CLI uses Ink (React for CLIs). It talks to the router API at `http://localhost:8080` by default.
Set the `LLM_ROUTER_URL` environment variable to change the backend URL.
