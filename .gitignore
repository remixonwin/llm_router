# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments / local env files (keep project source but ignore envs)
.venv/
.env
env/
venv/
uv.lock

# Node CLI build artifacts (exclude CLI dist from packaging)
/cli/dist/
/cli/node_modules/

# Dist and packaging
dist/
*.egg-info/

# Logs and runtime files
router.log
*.log

# IDE
.vscode/
.idea/
router.log

# Exclude CLI source from release packaging; the CLI is under active
# development and currently has a known backend-integration bug. Keep the
# CLI directory in the repo but exclude it from release artifacts.
/cli/
